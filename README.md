1) Python version 3.10.11
2) Create virtual environment - "python -m venv .venv" and ".venv\Scripts\activate"
3) Install basic libraries of any versions pandas, numpy.
4) Install libraries in requirements.txt -"pip install -r requirements.txt"
5) Run - "streamlit run app.py"
6) Upload videos to see the application performance.
